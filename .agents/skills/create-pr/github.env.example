# Copie vers .github.env (gitignored) et remplis le token.
# GitHub → Settings → Developer settings → Personal access tokens
#   → Fine-grained, repo spring-boot-3-kotlin-demo-corouter, "Pull requests: Read and write".

GITHUB_TOKEN=github_pat_xxx
